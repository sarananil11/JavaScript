//forEach doesn't return anything. so we just called 'num' and logged it.
const numbers = [1, 2, 3];

numbers.forEach(num => {  // curly brace becaus it is multi line.
    console.log(num);

});

// or you can also do : 

// numbers.forEach(num=> console.log(num)
// );